# Map submodules to packages 

  subdmodule-definitions: 
  
    SHARED: [domain.shared, app.dto, infrastructure.adapters.driving.persistence.shared]
    GAME: [domain.game, app.service.game, infrastructure.adapters.driving.persistence.game]
    TABLE: [domain.table, app.service.seating, app.service.query, infrastructure.adapters.driving.persistence.table]
    PLAYER: [domain.player, app.service.registration, infrastructure.adapters.driving.persistence.player]

# Define dependencies between submodules

  subdmodule-dependencies: 

    SHARED: []
    GAME: [SHARED]
    TABLE: [SHARED]
    PLAYER: [SHARED]
